---
'@cherrystudio/ai-core': patch
---

Add optional per-provider-call observation hooks for image, embedding, and rerank runtimes, including invocation identity, usage when available, and completion metrics for per-invocation usage and performance projections.
